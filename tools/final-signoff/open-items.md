# Open items — final sign-off

Classification: BLOCKER · ENVIRONMENTAL (real, needs lab/infra to close) · NON_BLOCKING · DEFERRED.

| ID | Class | Item | Why open / next step |
|----|-------|------|----------------------|
| OI-1 | **ENVIRONMENTAL** | **VMware Tools not installed by the base build** (`packer/base`). First-boot `Get-GuestInfo` reads the runner token via `vmtoolsd`; a Tools-less image would fail every clone's first boot. | A **build-gate guard** now fails the build if `vmtoolsd` is absent (so it can't silently ship). To close: either (a) add an unattended VMware Tools install to `packer/base` and verify `vmtoolsd.exe` before convert-to-template, **or** (b) confirm the Aria/vSphere platform (catalog cloud-template) already provides Tools. Both need the **lab vCenter** to author/verify. |
| OI-2 | **DEFERRED** | **autounattend Administrator password ↔ `PKR_VAR_ssh_password` coupling** (`packer/base/autounattend.xml` + `variables.pkr.hcl`). Must be kept equal manually or the base build's SSH connect fails. | Documented (XML comment + README); fails loud (SSH timeout). Optional robust fix: render `autounattend.xml` via `templatefile()` from `var.ssh_password` (single source) — needs lab verification; not done to avoid an unverifiable new bug. Owner de-prioritized; simplicity preferred. |
| OI-3 | **ENVIRONMENTAL** | **`vm_inputs` string→catalog-schema coercion** unproven. `vm_inputs` is `map(string)`; the request relies on Aria coercing `"24"`/`"true"` to the blueprint's typed inputs. | Confirm each key/type against the **real Service Broker catalog item schema** with the catalog owner; test a `plan`/`apply` in a sandbox. Cannot be verified without the live catalog. |
| OI-4 | **ENVIRONMENTAL** | **Lab smoke-test before fleet rollout.** `terraform`/`packer validate` pass here, but a full `packer build` + `terraform apply` + first-boot registration have not run on real infra. | Run one end-to-end build + single-runner deploy on the lab vCenter; assert reboot-resume, first-boot register, `gitlab-runner verify`. |
| OI-5 | **NON_BLOCKING** | **Transfer scripts `$RepoRoot` param-default uses `$PSScriptRoot`**, which is empty under a relative `-File` invocation → `Split-Path` error. | Invoke `Export-/Import-Transfer.ps1` with an **absolute path + explicit `-RepoRoot`** (documented in the airgap-deploy skill + the USB package notes). Low-risk to harden the default later. |
| OI-6 | **NON_BLOCKING** | **5 convenience tools not gathered** (SystemInformer canary, BareTail, WizTree, EventLook, OpenCode-desktop) + docker README says `25.0.15` (typo; actual `25.0.5`). | `Install-Tools` tolerates missing tools (non-fatal). Fetch + add to `tools/` if wanted; correct the README version note. |

**No BLOCKER-class items remain that are fixable from the repo.** OI-1/OI-3/OI-4 are genuine environmental items requiring the lab/live Aria; OI-2 is a documented deferred coupling; OI-5/OI-6 are cosmetic/operational.
