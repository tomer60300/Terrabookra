# Codex findings — triage

Independent reviewer: **Codex via MCP** (`chatgpt-bridge`, session `roadrunner-final-signoff-80f7e5`, backed by Codex CLI which read the worktree files directly). Reviewed in 3 scoped batches (the full-repo single pass exceeded the 180 s bridge timeout).

Classification key: ACCEPT_FIX · REJECT_NOT_A_BUG · REJECT_OVER_SOLVING · REJECT_CONSTRAINT_VIOLATION · DEFER_NON_BLOCKING · NEEDS_MANUAL_INFO.

| ID | Sev | File | Codex claim | Classification | Reason |
|----|-----|------|-------------|----------------|--------|
| BATCH 1 | — | terraform/ + module/aria-vm/ | *no actionable defects* | — | Terraform/Aria IaC clean (independent). |
| PS-PRE-001 | med | `scripts/Test-AriaTerraformPreflight.ps1:115` | `& $TerraformExe version 2>&1` under `EAP=Stop` can throw on stderr before `$LASTEXITCODE` is read | **ACCEPT_FIX** | Real, same class fixed elsewhere; I had flagged it myself. Fixed by a local `EAP=Continue` wrap. |
| PS-BOOT-001 | high | `provisioners/Register-RunnerFirstBoot.ps1:276` | `--registration-token $token` puts the secret on the process command line | **REJECT_OVER_SOLVING / CONSTRAINT** | The token is **not logged** (`Invoke-RunnerNative` logs command *output*, not args). `--registration-token` is the **standard, required** `gitlab-runner register` flag. Codex's fix ("remove the PAT/registration-token branch") **violates decision 1** (must support glrt- *and* PAT). argv is not a git/logs/tfvars leak (the stated constraint). Owner explicitly de-prioritized security; the token is a one-time registration token used at SYSTEM first boot on an air-gapped single-tenant box. Not a defect under this project's constraints. |
| PS-BOOT-002 | med | `provisioners/Register-RunnerFirstBoot.ps1:68-73` | `Invoke-RunnerNative` fail-opens (returns 0) if the exe can't be resolved | **REJECT_NOT_A_BUG** | **Empirically refuted** on PS 5.1.26100: `& 'missing.exe'` under `EAP=Continue` throws `CommandNotFoundException` (terminating) — it does NOT return exit 0. The throw propagates to `Invoke-Registration`'s retry `try/catch` → `$false` → retry. No fail-open. (Verified — see verification-log.) |
| PKR-BASE-001 | blocker | `packer/base/autounattend.xml` + `base.pkr.hcl` + `variables.pkr.hcl` | The baked local-Administrator password and Packer `ssh_password` are set in two places with no coupling; a mismatch fails the base-build SSH connect | **DEFER_NON_BLOCKING** | Real coupling, but it is a **documented, fail-loud, one-time build-host** contract (the XML comment + README state the two must match). The complete fix (render `autounattend.xml` via `templatefile()` from `var.ssh_password`) **cannot be verified without a lab vCenter** and risks a new bug (XML escaping / floppy_content behavior). Not a code defect on a correct invocation → not a sign-off blocker. Documented in open-items. |
| PKR-BASE-002 | blocker | `packer/base/*` | The VMware Tools ISO is mounted but Tools is never installed; first-boot `Get-GuestInfo` needs `vmtoolsd` → every clone fails to self-register | **NEEDS_MANUAL_INFO + partial fix** | **Confirmed** real gap: a repo-wide grep found nothing installs VMware Tools, yet `Get-GuestInfo` requires `vmtoolsd`. Whether it hard-blocks depends on whether the Aria/vSphere platform provides Tools (unverifiable from the repo). **Partial fix applied**: a build-gate check (`vmtoolsd present`) so a Tools-less image fails the *build*, not every clone's first boot. **Root-cause remediation** (add an unattended Tools install to `packer/base` + verify) needs a lab to author/test safely → open-item. |

## Own review (Claude Code)
Concur with the above. No additional blocker/high defects beyond this session's prior deep reviews (all confirmed blockers already fixed in `76193f2..052ee52`). The independent Codex pass added genuine value: **PKR-BASE-002 (VMware Tools)** is the most important new finding. `terraform fmt` re-run during verification also caught an unformatted `example.tfvars` (introduced by an earlier placeholder edit) — fixed.
