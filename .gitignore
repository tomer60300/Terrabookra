# Binaries (stored in MinIO, not Git — too large for GitHub)
binaries/docker/
binaries/git/
binaries/*.exe
tools/*.exe
tools/*.zip
tools/sysinternals/

# Credentials
*.env
secrets.json
# Real Packer/Terraform secret var-files (example.pkrvars.hcl IS committed)
*.auto.pkrvars.hcl
secret.pkrvars.hcl
*.auto.tfvars
terraform.tfvars
# Terraform state + local dirs
.terraform/
*.tfstate
*.tfstate.*

# Logs
*.log
*.log.old

# Temp files
*.tmp
~$*

# Agent / workflow logs
agent-logs/

# OS
Thumbs.db
desktop.ini
.DS_Store
